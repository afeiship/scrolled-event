import ScrolledEvent from '../src';

// ReferenceError: Can't find variable: window
// spy window

describe('api.basic', () => {
  test('Normal single value case', () => {
    // expect(typeof ScrolledEvent.on).toBe('function');
  });
});
